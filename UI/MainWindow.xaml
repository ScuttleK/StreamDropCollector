<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Stream Drop Collector"
        MinHeight="750" Height="750"
        MinWidth="1200" Width="1200"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        Background="{DynamicResource MainBackgroundBrush}"
        WindowStartupLocation="CenterScreen">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="1" ResizeBorderThickness="5" CaptionHeight="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>

        <!-- ── SIDEBAR NAV (RadioButton group, one active at a time) ── -->
        <Style x:Key="NavButtonStyle" TargetType="RadioButton">
            <Setter Property="Background"               Value="Transparent"/>
            <Setter Property="Foreground"               Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontFamily"               Value="{DynamicResource UiFont}"/>
            <Setter Property="FontSize"                 Value="13"/>
            <Setter Property="FontWeight"               Value="Medium"/>
            <Setter Property="Margin"                   Value="8,2"/>
            <Setter Property="Height"                   Value="40"/>
            <Setter Property="Padding"                  Value="14,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor"                   Value="Hand"/>
            <Setter Property="FocusVisualStyle"         Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"
                                              TextBlock.FontFamily="{TemplateBinding FontFamily}"
                                              TextBlock.FontSize="{TemplateBinding FontSize}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Active page -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <!-- Hover (not active) -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked"   Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── TITLE-BAR BUTTONS ── -->
        <Style x:Key="TitleBarMinMaxButton" TargetType="Button">
            <Setter Property="Background"       Value="Transparent"/>
            <Setter Property="Foreground"       Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="FontSize"         Value="13"/>
            <Setter Property="Width"            Value="40"/>
            <Setter Property="Height"           Value="44"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource TitleBarButtonHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseBtn" TargetType="Button" BasedOn="{StaticResource TitleBarMinMaxButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ═══════════════════════════════════════
             SIDEBAR
             ═══════════════════════════════════════ -->
        <Border Grid.Column="0"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="56"/>   <!-- App identity -->
                    <RowDefinition Height="*"/>    <!-- Nav items -->
                    <RowDefinition Height="Auto"/> <!-- Version -->
                </Grid.RowDefinitions>

                <!-- App identity (drag area) -->
                <Border Grid.Row="0" Padding="16,0"
                        MouseLeftButtonDown="OnWindowHeaderMouseDown"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="28" Height="28" CornerRadius="8"
                                Background="{DynamicResource AccentBrush}" Margin="0,0,10,0">
                            <TextBlock Text="S" FontWeight="Bold" FontSize="14"
                                       Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="StreamDrop" FontWeight="SemiBold" FontSize="13"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Nav items -->
                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                    <RadioButton Content="Dashboard" Tag="Dashboard" GroupName="Nav"
                                 IsChecked="True"
                                 Click="SidebarButton_Click"
                                 Style="{StaticResource NavButtonStyle}"/>
                    <RadioButton Content="Inventory" Tag="Inventory" GroupName="Nav"
                                 Click="SidebarButton_Click"
                                 Style="{StaticResource NavButtonStyle}"/>
                    <RadioButton Content="Settings"  Tag="Settings"  GroupName="Nav"
                                 Click="SidebarButton_Click"
                                 Style="{StaticResource NavButtonStyle}"/>
                    <RadioButton Content="Help"      Tag="Help"      GroupName="Nav"
                                 Click="SidebarButton_Click"
                                 Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>

                <!-- Version -->
                <TextBlock Grid.Row="2"
                           Text="{Binding VersionString, StringFormat='v{0}', FallbackValue='v1.0'}"
                           Foreground="{DynamicResource TextMutedBrush}"
                           FontSize="11" Margin="16,0,16,14"/>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════
             RIGHT SIDE: thin header + content
             ═══════════════════════════════════════ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Thin drag + window-control bar -->
            <Border Grid.Row="0"
                    Background="{DynamicResource MainBackgroundBrush}"
                    MouseLeftButtonDown="OnWindowHeaderMouseDown"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Name="Min_Button"        Content="⎯"  Click="OnMinimizeButtonClicked"  Style="{StaticResource TitleBarMinMaxButton}" ToolTip="Minimize"/>
                    <Button x:Name="Maximize_Button" Content="⬜" Click="OnMaximizeButtonClicked"  Style="{StaticResource TitleBarMinMaxButton}" ToolTip="Maximize"/>
                    <Button Name="Close_Button"      Content="✕"  Click="OnCloseButtonClicked"     Style="{StaticResource CloseBtn}"              ToolTip="Close"/>
                </StackPanel>
            </Border>

            <!-- Main content area -->
            <Border Grid.Row="1" Background="{DynamicResource MainBackgroundBrush}">
                <ContentControl x:Name="MainContent"/>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════
             SYSTEM TRAY ICON
             ═══════════════════════════════════════ -->
        <tb:TaskbarIcon x:Name="MyNotifyIcon"
                        IconSource="/Assets/logo.png"
                        ToolTipText="Stream Drop Collector by tsgsOFFICIAL"
                        DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Visibility="Collapsed">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Stream Drop Collector" IsEnabled="False" Foreground="LightGray">
                        <MenuItem.Icon>
                            <Canvas Width="24" Height="24">
                                <Image Source="/Assets/logo.png" Width="24" Height="24" Stretch="Uniform" Margin="6,0"/>
                            </Canvas>
                        </MenuItem.Icon>
                    </MenuItem>

                    <Separator Margin="0"/>

                    <MenuItem x:Name="JoinDiscord" Header="Join Discord" Command="{Binding JoinDiscordCommand}">
                        <MenuItem.Icon>
                            <Canvas Width="24" Height="24">
                                <Image Source="/Assets/discord.png" Width="24" Height="24" Stretch="Uniform" Margin="6,0"/>
                            </Canvas>
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem x:Name="OpenGithub" Header="Open Github" Command="{Binding OpenGithubCommand}">
                        <MenuItem.Icon>
                            <Canvas Width="24" Height="24">
                                <Image Source="/Assets/github.png" Width="24" Height="24" Stretch="Uniform" Margin="6,0"/>
                            </Canvas>
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem x:Name="MinimizeAndRestore" Header="Minimize" Command="{Binding ToggleWindowCommand}"/>
                    <MenuItem Header="Exit" Command="{Binding CloseCommand}"/>

                    <ContextMenu.Resources>
                        <Style TargetType="MenuItem">
                            <Setter Property="Background"   Value="#1A1D2A"/>
                            <Setter Property="Foreground"   Value="#ECEEF1"/>
                            <Setter Property="FontFamily"   Value="Segoe UI Variable Display, Segoe UI"/>
                            <Setter Property="FontSize"     Value="13"/>
                            <Setter Property="Padding"      Value="10,6"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin"       Value="0,1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type MenuItem}">
                                        <Border x:Name="templateRoot"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                SnapsToDevicePixels="True">
                                            <Grid VerticalAlignment="Center">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter x:Name="Icon" ContentSource="Icon"
                                                                  HorizontalAlignment="Center"
                                                                  Height="16" Width="16"
                                                                  Margin="2,5,5,5"
                                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                  VerticalAlignment="Center"/>
                                                <ContentPresenter ContentSource="Header" Grid.Column="1"
                                                                  Margin="{TemplateBinding Padding}"
                                                                  RecognizesAccessKey="True"
                                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="Icon" Value="{x:Null}">
                                                <Setter Property="Visibility" TargetName="Icon" Value="Collapsed"/>
                                            </Trigger>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter Property="Background" TargetName="templateRoot" Value="#242840"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="TextElement.Foreground" TargetName="templateRoot" Value="#6C7480"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style TargetType="ContextMenu">
                            <Setter Property="Background"       Value="#1A1D2A"/>
                            <Setter Property="BorderBrush"      Value="#242840"/>
                            <Setter Property="BorderThickness"  Value="1"/>
                            <Setter Property="Padding"          Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ContextMenu">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8"
                                                SnapsToDevicePixels="True"
                                                Padding="6">
                                            <ItemsPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Separator}">
                                        <Border Height="1" Margin="4,4,4,0" Background="#242840" HorizontalAlignment="Stretch"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ContextMenu.Resources>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

    </Grid>
</Window>
