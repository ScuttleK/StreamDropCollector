<UserControl x:Class="UI.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:UI"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="{DynamicResource MainBackgroundBrush}">

    <UserControl.Resources>
        <!-- SLIM ROUNDED PROGRESS BAR -->
        <Style x:Key="SlimProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" ClipToBounds="True">
                            <Grid>
                                <Rectangle x:Name="PART_Track" Fill="Transparent"/>
                                <Border x:Name="PART_Indicator"
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="4"
                                        HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- SMALL STATUS PILL (shared geometry, color set per-instance) -->
        <Style x:Key="StatusPillStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>

        <!-- ACCENT ACTION BUTTON -->
        <Style x:Key="WatchButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Text="Inventory"
               FontSize="30"
               FontWeight="Bold"
               Foreground="{DynamicResource TextPrimaryBrush}"
               Margin="0,0,0,20"/>

        <!-- CAMPAIGNS LIST -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ActiveCampaigns}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundBrush}"
                                CornerRadius="14"
                                Margin="0,0,0,20"
                                Padding="24"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="320"/>
                                    <ColumnDefinition Width="48"/>
                                    <ColumnDefinition Width="420"/>
                                </Grid.ColumnDefinitions>

                                <!-- Game Image -->
                                <Border Width="76" Height="76" CornerRadius="10" VerticalAlignment="Top">
                                    <Border.Background>
                                        <SolidColorBrush Color="{DynamicResource CardBackgroundColor}"/>
                                    </Border.Background>

                                    <Border.Clip>
                                        <RectangleGeometry RadiusX="10" RadiusY="10" Rect="0,0,76,76"/>
                                    </Border.Clip>

                                    <Image local:ImageEx.SourceUrl="{Binding GameImageUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>

                                <!-- Campaign Info -->
                                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Top">
                                    <TextBlock Text="{Binding GameName}"
                                               FontSize="22"
                                               FontWeight="Bold"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>

                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               Margin="0,4,0,12"/>

                                    <!-- Platform Badge + End Time + Watching Badge -->
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- Platform Badge - Kick = default (green), Twitch = purple -->
                                        <Border>
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource StatusPillStyle}">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <SolidColorBrush Color="{DynamicResource KickGreen}"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Platform}" Value="Twitch">
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <SolidColorBrush Color="{DynamicResource TwitchPurple}"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <TextBlock Text="{Binding Platform}"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               FontSize="11"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Ends pill -->
                                        <Border Style="{StaticResource StatusPillStyle}"
                                                Background="{DynamicResource SurfaceMutedBrush}"
                                                Margin="8,0,0,0">
                                            <TextBlock Foreground="{DynamicResource DangerBrush}" FontSize="11" FontWeight="SemiBold">
                                                <Run Text="Ends "/>
                                                <Run Text="{Binding EndsAt, StringFormat=MMM dd • HH:mm}"/>
                                            </TextBlock>
                                        </Border>

                                        <!-- WATCHING Badge -->
                                        <Border Style="{StaticResource StatusPillStyle}"
                                                Margin="8,0,0,0"
                                                Background="{DynamicResource AccentBrush}"
                                                Visibility="{Binding IsCurrentCampaign, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Text="WATCHING"
                                                       FontWeight="Bold"
                                                       FontSize="10"
                                                       Foreground="{DynamicResource OnAccentBrush}"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- Watch button - only shown when NOT the current campaign -->
                                    <Button Content="▶  Watch this campaign instead"
                                        Margin="0,14,0,0"
                                        HorizontalAlignment="Left"
                                        Command="{Binding DataContext.SwitchCampaignCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource WatchButtonStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsCurrentCampaign}" Value="False"/>
                                                            <Condition Binding="{Binding AllRewardsClaimed}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>

                                <!-- Rewards List -->
                                <StackPanel Grid.Column="3" VerticalAlignment="Top">
                                    <TextBlock Text="Rewards"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,0,0,10"/>

                                    <ItemsControl ItemsSource="{Binding Rewards}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource SurfaceMutedBrush}"
                                                        CornerRadius="10"
                                                        Margin="0,0,0,8"
                                                        Padding="12"
                                                        BorderThickness="1"
                                                        BorderBrush="{DynamicResource BorderBrush}">

                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <!-- Highlight active reward -->
                                                                <DataTrigger Binding="{Binding IsCurrentReward}" Value="True">
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                                    <Setter Property="BorderThickness" Value="1.5"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>

                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- Reward Image -->
                                                        <Border Grid.Column="0" Width="46" Height="46" CornerRadius="8">
                                                            <Border.Background>
                                                                <SolidColorBrush Color="{DynamicResource CardBackgroundColor}"/>
                                                            </Border.Background>

                                                            <Border.Clip>
                                                                <RectangleGeometry RadiusX="8" RadiusY="8" Rect="0,0,46,46"/>
                                                            </Border.Clip>

                                                            <Image local:ImageEx.SourceUrl="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                                        </Border>

                                                        <!-- Name + Progress -->
                                                        <StackPanel Grid.Column="1" Margin="12,0,10,0" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Name}"
                                                                       FontWeight="SemiBold"
                                                                       FontSize="13"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       Foreground="{DynamicResource TextPrimaryBrush}"/>

                                                            <TextBlock Margin="0,5,0,0" FontSize="11">
                                                                <Run Text="{Binding ProgressMinutes}" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                                                                <Run Text=" / " Foreground="{DynamicResource TextMutedBrush}"/>
                                                                <Run Text="{Binding RequiredMinutes}" Foreground="{DynamicResource TextMutedBrush}"/>
                                                                <Run Text=" min" Foreground="{DynamicResource TextMutedBrush}"/>
                                                            </TextBlock>

                                                            <!-- Progress Bar -->
                                                            <ProgressBar Value="{Binding ProgressMinutes}"
                                                                         Maximum="{Binding RequiredMinutes}"
                                                                         Style="{StaticResource SlimProgressBarStyle}"
                                                                         Margin="0,7,0,0"/>

                                                            <!-- Most recent failed claim attempt, if any -->
                                                            <Border Margin="0,5,0,0" Padding="6,4" CornerRadius="6"
                                                                    Background="#33E5534B"
                                                                    BorderBrush="{DynamicResource DangerBrush}"
                                                                    BorderThickness="1">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding ClaimErrorDisplayText}" Value="">
                                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition Width="*"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBlock Grid.Column="0" Text="⚠" FontSize="12" FontWeight="Bold"
                                                                               Foreground="{DynamicResource DangerBrush}"
                                                                               Margin="0,0,6,0" VerticalAlignment="Top"/>
                                                                    <TextBlock Grid.Column="1" Text="{Binding LastClaimError}" FontSize="11" FontWeight="SemiBold"
                                                                               Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap"/>
                                                                </Grid>
                                                            </Border>
                                                        </StackPanel>

                                                        <!-- CLAIMED Badge -->
                                                        <Border Grid.Column="2"
                                                                Style="{StaticResource StatusPillStyle}"
                                                                Background="{DynamicResource SuccessBrush}"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Padding="8,3"
                                                                Visibility="{Binding IsClaimed, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <TextBlock Text="CLAIMED"
                                                                       FontWeight="Bold"
                                                                       FontSize="9"
                                                                       Foreground="{DynamicResource OnAccentBrush}"/>
                                                        </Border>

                                                        <!-- WATCHING Badge -->
                                                        <Border Grid.Column="2"
                                                                Style="{StaticResource StatusPillStyle}"
                                                                Background="{DynamicResource AccentBrush}"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Padding="8,3"
                                                                Visibility="{Binding IsCurrentReward, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <TextBlock Text="WATCHING"
                                                                       FontWeight="Bold"
                                                                       FontSize="9"
                                                                       Foreground="{DynamicResource OnAccentBrush}"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <!-- EMPTY STATE - 100% SAFE -->
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <ItemsPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveCampaigns.Count}" Value="0">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <TextBlock Text="No active drops campaigns"
                                                       FontSize="26"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
